import 'package:flutter/material.dart' show Colors;

// ignore: camel_case_types
class k {
  static var error = false; //used to make fake error
  static var pinkColor = Colors.pinkAccent.withAlpha(200);
}
